# Core extraction and model clients
anthropic
openai

# Image and document processing
pillow
numpy
# Pinned to match every CV pipeline vendor drop's own requirements.txt
# (v1/v2/v2_2_1/v2_2_1_2_computer_vision) — OpenCV 5.x changes numeric
# internals enough to shift borderline geometry scores vs. what the vendor
# tested against.
opencv-python-headless>=4.9,<5
scipy

# API layer
fastapi
pydantic
gunicorn
uvicorn
python-multipart

# Azure integrations
azure-identity
azure-keyvault-secrets
azure-storage-blob

# Database
psycopg2-binary

# PDF splitting helper
pypdf
pymupdf

# Environment loading
python-dotenv